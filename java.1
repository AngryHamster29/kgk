import java.util.Random;
import java.util.Scanner;

public class Main {

    public static void main(String[] arqs) {


        boolean oj;
        boolean ataka;
        boolean shild;
        int dragon = 1000;
        int player = 200;
        int zel = 4;
        Scanner scan = new Scanner(System.in);

        Random random = new Random();


        System.out.println("Битва с драконом");
        System.out.println("У вас три действия:1-атака 2-закрыться шитоом 3-выпить зелье(их всего 4)");
        System.out.println("Битва начинается!");
        while (dragon > 0) {
            System.out.println("Ваш Ход, выберите действие");
            int number = scan.nextInt();

            if (number == 1) {
                ataka = random.nextBoolean();
                if (ataka == true) {
                    dragon = dragon - (1 + (int) (Math.random() * 76));
                    System.out.println("Осталось нанести урона:" + dragon);
                } else {
                    System.out.println("Промах");
                }
            } else {
                if (number == 3) {
                    if (zel >= 1) {
                        player = player + 200;
                        zel = zel - 1;
                    } else {
                        System.out.println("Зелья кончились(");
                    }
                }
            }
            System.out.println("Ход дракона");
            if (number == 2) {
                shild = random.nextBoolean();
                if (shild==true){
                    System.out.println("Вы заблокировали удар");
                }else {
                    player = player - (1 + ((int) (Math.random() * 60))/2);
                    System.out.println("Вы частично заблокировали удар");
                    System.out.println("Ваш уровень здоровья:" + player);

                }


            } else {
                player = player - (1 + ((int) (Math.random() * 60)));
                System.out.println("Ваш уровень здоровья:" + player);
            }
            if (player < 0) {
                break;
            }



        }
        System.out.println("Игра Окончена!");
    }
}
